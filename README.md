# BigIntFromPJH

## 1.2版本日志

### 一、重载了负号

现在可以对 $bigint$、$modint$ 进行取反了。

### 二、更新了 $byte$，其内部实现为bitset<4>

$byte$ 用于优化每个 $short$ 位带来的常数（效果未知）。

这个版本 $byte$ 是 $[-9,9]$ 的，为了优化符号位下个版本可能要改成 $[-7,7]$（存入一个字节内）
